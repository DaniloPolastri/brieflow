spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/briefflow
    username: briefflow
    password: briefflow

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

app:
  cors:
    allowed-origins: http://localhost:4200
